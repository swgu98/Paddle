name: workflow-run-test

on:
  pull_request:
    branches: [ "develop" ]
  workflow_run:
    workflows: [Codestyle-Check]
    types:
      - completed

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: |
        echo "hh"
