name: workflow-run-test

on:
  pull_request:
  workflow_run:
    workflows: [Codestyle-Check]
    types: [completed]
    branches: [ "develop" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: hh
      run: |
        echo "hh"
